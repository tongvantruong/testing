name: (dev) auto-upload-external-lib

on:
  pull_request:
    types: opened

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: testing 2
        run: |
          ls truong/test.txt
          mkdir truong
          cd truong
          touch test.txt
          ls truong/test.txt
